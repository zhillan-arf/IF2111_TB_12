/*
    TUBES IF2111 K2 KELOMPOK 12
    COMMAND ROLL - UNIT TEST
    Versi: 2021-11-15 11:30
*/

#include "../commands/roll.h"
#include <stdio.h>
/*
int main () {
    // KAMUS
    int Roll, i, repeat = 20;
    // ALGORTIMA
    for(i = 0; i < repeat; i++)
    {
        roll(&Roll, 10, true, false);
        printf("TEST1 isBesar rolled: %d\n", Roll);
    }
    for(i = 0; i < repeat; i++)
    {
        roll(&Roll, 10, false, true);
        printf("TEST2 isKecil rolled: %d\n", Roll);
    }
    for(i = 0; i < repeat; i++)
    {
        roll(&Roll, 10, false, false);
        printf("TEST3 normal rolled: %d\n", Roll);
    }
}
*/